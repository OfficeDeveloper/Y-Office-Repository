<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.office.officegame" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19">
        </uses-sdk>

    <application


        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChooseGameMenu"
            android:label="ChooseGameMenu">

            </activity>
        <activity
            android:name=".SettingsMenu"
            android:label="SettingsMenu">

        </activity>

        <activity
            android:name=".Firstgame"
            android:label="Arcade">

        </activity>

        <activity
            android:name=".ThirdGame"
            android:label="Time Attack">
        </activity>

        <activity
            android:name=".SecondGame"
            android:label="Time Sprint">
        </activity>

    </application>

</manifest>
